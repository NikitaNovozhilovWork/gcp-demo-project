spring:
  cloud:
    gcp:
      config:
        name: gitterchat
        profile: kubernetes
        enabled: true
        # Config server API timeout in milliseconds, default : 60000 (1 minute)
        timeout-millis: 120000
